{
  "version": 1,
  "description": "Python packages preinstalled into the bundled Windows Python runtime at build time, so shipped skills work on a clean install without network access. This table is the single source of truth and versions must be exact '==' pins; pip resolves transitive dependencies at build time. Add a package with a '==0.0.0' placeholder and run: npm run skill-python-deps:resolve",
  "target": {
    "platform": "win_amd64",
    "implementation": "cp",
    "pythonVersion": "311"
  },
  "packages": {
    "requests": "==2.34.2",
    "lxml": "==6.1.3",
    "defusedxml": "==0.7.1",
    "openpyxl": "==3.1.5",
    "Pillow": "==12.3.0",
    "PyYAML": "==6.0.3",
    "six": "==1.17.0",
    "python-pptx": "==1.0.2",
    "python-docx": "==1.2.0",
    "pypdf": "==6.19.0",
    "pdf2image": "==1.17.0"
  }
}
